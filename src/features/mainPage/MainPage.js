import { SearchForm } from '../search/SearchForm';

export default function MainPage() {
    return (
        <div className="body">
            Главная Страница
            <SearchForm />
        </div>
    );
}
