import { Search } from '../search/Search';

export default function MainPage() {
    return (
        <div className="body">
            Главная Страница
            <Search />
        </div>
    );
}
